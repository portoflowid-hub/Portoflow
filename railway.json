{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ./portoflow-be && npm install"
  },
  "deploy": {
    "startCommand": "cd ./portoflow-be && npm start",
    "healthcheckPath": "/api",
    "healthcheckTimeout": 100
  }
}